# Commits

## Scope

- Applies to all code commits authored on my behalf.

## Directives

- NEVER add `Co-Authored-By:` on commits.
- NEVER use the `--author` flag to bypass the base user from the git credentials.
